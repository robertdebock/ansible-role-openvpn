---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: openvpn-debian-stable
    image: debian:stable
provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        openvpn_ignore_docker: no
  lint:
    name: ansible-lint
scenario:
  name: debian-stable
